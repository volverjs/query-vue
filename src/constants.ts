export enum StoreRepositoryStatus {
    loading = 'loading',
    error = 'error',
    success = 'success',
    idle = 'idle',
}

export enum StoreRepositoryAction {
    read = 'read',
    create = 'create',
    update = 'update',
    remove = 'remove',

}
